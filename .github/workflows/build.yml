name: Build App
on:
  pull_request:
    branches:
      - main
    types: [closed]

  workflow_dispatch:
    inputs:
      os:
        type: choice
        description: OS to build on. Ubuntu is faster, MacOS supports iOS builds, Windows is unsupported
        options:
          - macos-latest
          - ubuntu-latest
      platform:
        type: choice
        description: Platform to build for
        options:
          - android
          - ios
      profile:
        type: choice
        description: Build profile to use
        options:
          - development
          - preview
          - production

jobs:
  release:
    if: ${{ github.event.pull_request.merged }}
    runs-on: 'ubuntu-latest'
    outputs:
      upload_url: ${{ steps.releaseplease.outputs.upload_url }}
    steps:
      - name: 🏗 Setup repo
        uses: actions/checkout@v2
      - name: 🎉 Create release info
        uses: google-github-actions/release-please-action@v3
        id: releaseplease
        with:
          release-type: expo
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJSON(github) }}
        run: echo "$GITHUB_CONTEXT"
      - name: Dump steps context
        env:
          GITHUB_CONTEXT: ${{ toJSON(steps) }}
        run: echo "$GITHUB_CONTEXT"

  requirement:
    if: ${{ (github.event.pull_request.merged && github.event.pull_request.user.login == 'github-actions[bot]') || github.event_name == 'workflow_dispatch' }}
    runs-on: 'ubuntu-latest'
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJSON(github) }}
        run: echo "$GITHUB_CONTEXT"

  packageinfo:
    runs-on: 'ubuntu-latest'
    needs: requirement
    outputs:
      name: ${{ steps.package.outputs.name }}
      version: ${{ steps.package.outputs.version }}
    steps:
      - name: 🏗 Setup repo
        uses: actions/checkout@v2
      - name: 📦 Get package info
        id: package
        uses: codex-team/action-nodejs-package-info@v1.1
        with:
          path: ${{ github.workspace }}
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJSON(github) }}
        run: echo "$GITHUB_CONTEXT"
      - name: Dump steps context
        env:
          GITHUB_CONTEXT: ${{ toJSON(steps) }}
        run: echo "$GITHUB_CONTEXT"

  prebuild:
    runs-on: ${{ github.event.inputs.os || 'ubuntu-latest' }}
    needs: [requirement, release, packageinfo]
    strategy:
      matrix:
        node: [16.x]
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJSON(github) }}
        run: echo "$GITHUB_CONTEXT"
      - name: Dump needs
        env:
          GITHUB_CONTEXT: ${{ toJSON(needs) }}
        run: echo "$GITHUB_CONTEXT"

      - name: 🏗 Setup repo
        uses: actions/checkout@v2

      - name: 🏗 Setup Node
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node }}
          cache: yarn

      - name: 🏗 Setup Expo and EAS
        uses: expo/expo-github-action@v7
        with:
          token: ${{ secrets.EXPO_TOKEN }}
          expo-version: latest
          eas-version: latest

      - name: 🏗 Setup EAS local builds
        run: yarn global add eas-cli-local-build-plugin

      - name: 📦 Install dependencies
        run: yarn

      - name: 📱 Upload build environment for next stage
        uses: actions/upload-artifact@v3
        with:
          name: prebuild
          path: ./
          
  build:
    if: ${{ github.event_name == 'workflow_dispatch' }}
    runs-on: ${{ github.event.inputs.os || 'ubuntu-latest' }}
    needs: [requirement, release, packageinfo, prebuild]
    strategy:
      matrix:
        node: [16.x]
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJSON(github) }}
        run: echo "$GITHUB_CONTEXT"
      - name: Dump needs
        env:
          GITHUB_CONTEXT: ${{ toJSON(needs) }}
        run: echo "$GITHUB_CONTEXT"

      - name: 📱 Download build environment
        uses: actions/download-artifact@v3
        with:
          name: prebuild
          
      - name: 👷 Build app
        run: |
          eas build --local \
            --non-interactive \
            --output=./app-build \
            --platform=${{ github.event.inputs.platform || 'android' }} \
            --profile=production
            
      - name: 📱 Upload build environment for next stage
        uses: actions/upload-artifact@v3
        with:
          name: '${{ needs.packageinfo.outputs.name }}-${{ needs.packageinfo.outputs.version }}'
          path: app-build


  buildprod:
    if: ${{ github.event_name != 'workflow_dispatch' }}
    runs-on: ${{ github.event.inputs.os || 'ubuntu-latest' }}
    needs: [requirement, release, packageinfo, prebuild]
    strategy:
      matrix:
        node: [16.x]
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJSON(github) }}
        run: echo "$GITHUB_CONTEXT"
      - name: Dump needs
        env:
          GITHUB_CONTEXT: ${{ toJSON(needs) }}
        run: echo "$GITHUB_CONTEXT"

      - name: 📱 Download build environment
        uses: actions/download-artifact@v3
        with:
          name: prebuild
          
      - name: 👷 Build app
        run: |
          eas build --local \
            --non-interactive \
            --output=./app-build \
            --platform=${{ github.event.inputs.platform || 'android' }} \
            --profile=production

      - name: 👍 Attach build artifact to release
        if: ${{ needs.release.outputs.upload_url }}
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: '${{ needs.release.outputs.upload_url }}'
          asset_path: './app-build'
          asset_name: '${{ needs.packageinfo.outputs.name }}-${{ needs.packageinfo.outputs.version }}.apk'
          asset_content_type: application/octet-stream

  buildprev:
    if: ${{ github.event_name != 'workflow_dispatch' }}
    runs-on: ${{ github.event.inputs.os || 'ubuntu-latest' }}
    needs: [requirement, release, packageinfo, prebuild]
    strategy:
      matrix:
        node: [16.x]
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJSON(github) }}
        run: echo "$GITHUB_CONTEXT"
      - name: Dump needs
        env:
          GITHUB_CONTEXT: ${{ toJSON(needs) }}
        run: echo "$GITHUB_CONTEXT"

      - name: 📱 Download build environment
        uses: actions/download-artifact@v3
        with:
          name: prebuild
          
      - name: 👷 Build app
        run: |
          eas build --local \
            --non-interactive \
            --output=./app-build \
            --platform=${{ github.event.inputs.platform || 'android' }} \
            --profile=preview

      - name: 👍 Attach build artifact to release
        if: ${{ needs.release.outputs.upload_url }}
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: '${{ needs.release.outputs.upload_url }}'
          asset_path: './app-build'
          asset_name: '${{ needs.packageinfo.outputs.name }}-preview-${{ needs.packageinfo.outputs.version }}.apk'
          asset_content_type: application/octet-stream

  builddev:
    if: ${{ github.event_name != 'workflow_dispatch' }}
    runs-on: ${{ github.event.inputs.os || 'ubuntu-latest' }}
    needs: [requirement, release, packageinfo, prebuild]
    strategy:
      matrix:
        node: [16.x]
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJSON(github) }}
        run: echo "$GITHUB_CONTEXT"
      - name: Dump needs
        env:
          GITHUB_CONTEXT: ${{ toJSON(needs) }}
        run: echo "$GITHUB_CONTEXT"

      - name: 📱 Download build environment
        uses: actions/download-artifact@v3
        with:
          name: prebuild
          
      - name: 👷 Build app
        run: |
          eas build --local \
            --non-interactive \
            --output=./app-build \
            --platform=${{ github.event.inputs.platform || 'android' }} \
            --profile=development

      - name: 👍 Attach build artifact to release
        if: ${{ needs.release.outputs.upload_url }}
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: '${{ needs.release.outputs.upload_url }}'
          asset_path: './app-build'
          asset_name: '${{ needs.packageinfo.outputs.name }}-dev-${{ needs.packageinfo.outputs.version }}.apk'
          asset_content_type: application/octet-stream
